import type { AST, Fragment, Operation } from 'graphql-tool-utilities';
import type { Options } from './context';
export interface File {
    path: string;
    operation?: Operation;
    fragments: Fragment[];
}
export type { Options };
export declare function printDocument({ path, operation, fragments }: File, ast: AST, options: Options): string;
//# sourceMappingURL=index.d.ts.map