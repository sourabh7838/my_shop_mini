import { EventEmitter } from 'events';

class AbstractGraphQLFilesystem extends EventEmitter {
  once(event, handler) {
    return super.once(event, handler);
  }
  on(event, handler) {
    return super.on(event, handler);
  }
  emit(event, ...args) {
    return super.emit(event, ...args);
  }
}

export { AbstractGraphQLFilesystem };
