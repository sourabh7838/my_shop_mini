import { isGenerator as s } from "../generators/utils/is-generator.js";
import { isAnimatable as l } from "./is-animatable.js";
import { warning as f } from "../../../../../motion-utils/dist/es/errors.js";
function g(t) {
  const i = t[0];
  if (t.length === 1)
    return !0;
  for (let n = 0; n < t.length; n++)
    if (t[n] !== i)
      return !0;
}
function h(t, i, n, u) {
  const r = t[0];
  if (r === null)
    return !1;
  if (i === "display" || i === "visibility")
    return !0;
  const a = t[t.length - 1], e = l(r, i), o = l(a, i);
  return f(e === o, `You are trying to animate ${i} from "${r}" to "${a}". ${r} is not an animatable value - to enable this animation set ${r} to a value animatable to ${a} via the \`style\` property.`), !e || !o ? !1 : g(t) || (n === "spring" || s(n)) && u;
}
export {
  h as canAnimate
};
//# sourceMappingURL=can-animate.js.map
