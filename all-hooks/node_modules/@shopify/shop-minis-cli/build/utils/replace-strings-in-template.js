import replaceInFile from 'replace-in-file';
export async function replaceStringsInTemplate(dir, stringsToReplace) {
    for (const [replaceKey, replaceValue] of Object.entries(stringsToReplace)) {
        const replaceOptions = {
            files: [`${dir}/**/*.*`],
            from: new RegExp(replaceKey, 'g'),
            to: replaceValue,
            optionsForFiles: {
                ignore: ['**/node_modules/**'],
            },
        };
        await replaceInFile.replaceInFile(replaceOptions);
    }
}
//# sourceMappingURL=replace-strings-in-template.js.map