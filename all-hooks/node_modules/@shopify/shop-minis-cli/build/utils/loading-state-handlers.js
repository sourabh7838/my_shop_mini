import ora from 'ora';
export class LoadingStateHandlers {
    _loading = null;
    loadingStart = (logMessage, options = {}) => {
        if (this._loading) {
            this.loadingEnd(options.endPreviousLoadingWithStatus, options.endPreviousLoadingWithMessage);
        }
        this._loading = ora({ text: logMessage }).start();
    };
    loadingEnd = (endState = 'succeed', logMessage) => {
        if (!this._loading) {
            return;
        }
        this._loading[endState](logMessage);
        this._loading = null;
    };
    loadingUpdate = (text) => {
        if (this._loading) {
            this._loading.text = text;
        }
    };
}
//# sourceMappingURL=loading-state-handlers.js.map