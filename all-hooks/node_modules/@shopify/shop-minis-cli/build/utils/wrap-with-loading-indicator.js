import ora from 'ora';
export async function wrapWithLoadingIndicator(promise, { loadingMessage, errorMessage, successMessage, spinnerOptions }) {
    const loadingIndicator = ora(spinnerOptions);
    loadingIndicator.start(loadingMessage);
    try {
        const result = await promise;
        loadingIndicator.succeed(successMessage);
        return result;
    }
    catch (err) {
        loadingIndicator.fail(errorMessage);
        console.log('\n');
        throw err;
    }
}
//# sourceMappingURL=wrap-with-loading-indicator.js.map