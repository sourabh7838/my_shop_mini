import { getMiniManifest } from './minis-manifest.js';
export const LEGACY_STANDALONE_HANDLES = [
    'color-analyst',
    'dreamboard',
    'place',
    'test-mini',
];
export const isStandaloneMini = () => {
    const manifest = getMiniManifest();
    if (LEGACY_STANDALONE_HANDLES.includes(manifest.handle)) {
        return false;
    }
    return (manifest?.features ?? []).includes('STANDALONE');
};
//# sourceMappingURL=standalone.js.map