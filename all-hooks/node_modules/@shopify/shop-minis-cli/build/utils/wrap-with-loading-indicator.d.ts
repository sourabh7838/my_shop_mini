import { Options as OraOptions } from 'ora';
interface LoadingOptions {
    loadingMessage: string;
    successMessage?: string;
    errorMessage?: string;
    spinnerOptions?: Omit<OraOptions, 'text' | 'prefixText'>;
}
export declare function wrapWithLoadingIndicator<T>(promise: Promise<T>, { loadingMessage, errorMessage, successMessage, spinnerOptions }: LoadingOptions): Promise<T>;
export {};
