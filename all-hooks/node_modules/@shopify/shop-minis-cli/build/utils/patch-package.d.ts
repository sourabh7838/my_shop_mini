/**
 * Applies patches to Minis dependencies (only if they have not been applied yet)
 * @returns a list with the names of the patches that were applied
 */
export declare function maybeApplyPatches(): string[];
