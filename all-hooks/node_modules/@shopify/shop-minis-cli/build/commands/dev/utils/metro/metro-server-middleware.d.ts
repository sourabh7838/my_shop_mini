import http from 'http';
import connect from 'connect';
import { DevCommandOptions } from '../../react-native.js';
/**
 * mutates the received metro config param to inject a custom middleware.
 * needed because of https://github.com/facebook/metro/issues/660
 */
export declare function buildMetroServerMiddleware(options: DevCommandOptions): connect.Server;
export declare const DEV_PANEL_BASE_URL = "http://localhost:8082/dev-panel";
export declare function inputQueryMiddlewareFactory(options: DevCommandOptions): (req: http.IncomingMessage, res: http.ServerResponse) => void;
