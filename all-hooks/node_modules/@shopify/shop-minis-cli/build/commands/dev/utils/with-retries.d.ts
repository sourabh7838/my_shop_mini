export declare function withRetries<T>(getResult: () => Promise<T>, isResultReady?: (result: T) => boolean, { maxRetries, timeBetweenRetries }?: {
    timeBetweenRetries: number;
    maxRetries: number;
}): Promise<T>;
