import { createRequire } from 'node:module';
import path from 'node:path';
/**
 * Read the version of the react-native package in the context of the running mini
 */
export function getReactNativeVersion() {
    try {
        const localRequire = createRequire(process.cwd());
        const rnDir = localRequire(path.join(process.cwd(), 'node_modules', 'react-native', 'package.json'));
        if (!rnDir?.version) {
            console.log('\n ⚠️  react-native version could not be found\n');
            return undefined;
        }
        return rnDir?.version;
    }
    catch (err) {
        console.log('\n ⚠️  react-native version could not be read due to errors:\n');
        console.log(err);
        console.log('\n');
        return undefined;
    }
}
//# sourceMappingURL=react-native-version.js.map