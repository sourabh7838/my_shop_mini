/**
 * Returns a custom `resolveRequest` function for metro that resolves `minis:widget` imports to
 * the component and `minis:seed` to the seeds of the widget specified in the `--widget-type` option.
 */
export function createMinisModuleResolver(existingMetroConfig) {
    return (context, moduleName, platform) => {
        const resolver = existingMetroConfig?.resolver?.resolveRequest ?? context.resolveRequest;
        return resolver(context, moduleName, platform);
    };
}
//# sourceMappingURL=metro-minis-module-resolver.js.map