import { Command } from 'commander';
export interface GenerateGraphqlTypesCommandOptions {
    watch?: boolean;
    verbose?: boolean;
}
export declare function loadCommand(parentProgram: Command): void;
