import { SemVer } from 'semver';
import { migrateToNewTheme } from './migrate-to-new-theme.js';
import { removeThemeFromConfig } from './remove-theme-from-config.js';
/**
 * Update partner code to use the new theme
 *
 * Diff: https://github.com/Shopify/shop-minis-cli/compare/v0.0.96...v0.0.97
 */
async function applyUpgrade() {
    await removeThemeFromConfig();
    await migrateToNewTheme();
}
const releaseNotes = ['Migrate to the new theme used in Shop'];
const exports = {
    releaseNotes,
    applyUpgrade,
    version: new SemVer('0.0.97'),
};
export default exports;
//# sourceMappingURL=index.js.map