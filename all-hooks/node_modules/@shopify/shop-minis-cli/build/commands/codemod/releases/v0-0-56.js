import fs from 'node:fs';
import path from 'node:path';
import { SemVer } from 'semver';
/**
 * Update types in assets.d.ts to get more specific suggestions for asset components
 *
 * Diff: https://github.com/Shopify/shop-minis-cli/compare/v0.0.55...v0.0.56
 */
async function applyUpgrade() {
    const assetsFilePath = path.join(process.cwd(), 'assets.d.ts');
    const assetsFileContent = fs.readFileSync(assetsFilePath, {
        encoding: 'utf-8',
    });
    const newAssetsContent = assetsFileContent.replace(oldAssetsTypes, newAssetsTypes);
    fs.writeFileSync(assetsFilePath, newAssetsContent, {
        encoding: 'utf-8',
    });
}
const oldAssetsTypes = `declare module '*.svg' {
  const content: any
  export default content
}
`;
const newAssetsTypes = `declare module '*.svg' {
  const content: import('react').FC<import('react-native-svg').SvgProps>
  export default content
}

declare module '*.png' {
  const content: import('react-native').ImageRequireSource
  export default content
}

declare module '*.jpg' {
  const content: import('react-native').ImageRequireSource
  export default content
}
`;
const releaseNotes = [
    'Update types in assets.d.ts for more specific typing on asset components',
];
const exports = {
    releaseNotes,
    applyUpgrade,
    version: new SemVer('0.0.56'),
};
export default exports;
//# sourceMappingURL=v0-0-56.js.map