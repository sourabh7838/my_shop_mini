import { readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { SemVer } from 'semver';
/**
 * Add graphql resolution in package.json
 *
 * Diff: https://github.com/Shopify/shop-minis-cli/compare/v0.0.105...v0.0.106
 */
async function applyUpgrade() {
    const packageJsonPath = path.join(process.cwd(), 'package.json');
    const packageJsonString = await readFile(packageJsonPath, {
        encoding: 'utf-8',
    });
    const packageJson = JSON.parse(packageJsonString) || {};
    packageJson.resolutions ||= {};
    packageJson.resolutions = {
        ...packageJson.resolutions,
        graphql: '15.8.0',
    };
    // persist updated package.json
    await writeFile(packageJsonPath, `${JSON.stringify(packageJson, null, 2)}\n`, // keep the new line at the end of the file
    {
        encoding: 'utf-8',
    });
}
const exports = {
    releaseNotes: ['Adds graphql resolution to package.json'],
    applyUpgrade,
    version: new SemVer('0.0.106'),
};
export default exports;
//# sourceMappingURL=v0-0-106.js.map