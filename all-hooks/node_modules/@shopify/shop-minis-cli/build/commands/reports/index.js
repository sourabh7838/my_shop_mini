import { Command } from 'commander';
import { command as listCommand } from './list.js';
import { command as datesCommand } from './dates.js';
import { command as getCommand } from './get.js';
export function loadCommand(parentProgram) {
    const command = new Command()
        .name('reports')
        .description('Manage reports for your Shop Mini.')
        .addCommand(listCommand)
        .addCommand(datesCommand)
        .addCommand(getCommand);
    parentProgram.addCommand(command);
}
//# sourceMappingURL=index.js.map