import { handler as handleError } from '@shopify/cli-kit/node/error';
import { validateUpgradeBetweenVersions } from '../../../utils/semver.js';
export function validateTargetCLIVersion(targetCliVersion, currentCliVersion) {
    if (targetCliVersion === 'latest') {
        return;
    }
    try {
        validateUpgradeBetweenVersions(currentCliVersion, targetCliVersion);
    }
    catch (err) {
        handleError(err);
        process.exit(1);
    }
}
//# sourceMappingURL=cli.js.map