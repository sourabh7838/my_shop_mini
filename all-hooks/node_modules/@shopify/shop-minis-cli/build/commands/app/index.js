import { Command } from 'commander';
import { isStandaloneMini } from '../../utils/standalone.js';
import { manifestExists } from '../../utils/minis-manifest.js';
import { command as listCommand } from './list.js';
import { command as linkCommand } from './link.js';
import { command as unlinkCommand } from './unlink.js';
export function loadCommand(parentProgram) {
    const command = new Command()
        .name('app')
        .description('Manage the Shopify apps that are linked to your Shop Mini.')
        .addCommand(listCommand)
        .addCommand(linkCommand)
        .addCommand(unlinkCommand);
    parentProgram.addCommand(command, {
        hidden: !manifestExists() || isStandaloneMini(),
    });
}
//# sourceMappingURL=index.js.map