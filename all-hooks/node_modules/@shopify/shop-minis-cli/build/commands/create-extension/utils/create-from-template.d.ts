import { ExtensionTarget, ExtensionType } from '../../../constants/extensions.js';
import { ExtensionPage } from '../types.js';
export declare function createExtensionFromTemplate(miniName: string, extensionPage: ExtensionPage, extensionTarget: ExtensionTarget, extensionType: ExtensionType, outputDir: string): Promise<void>;
