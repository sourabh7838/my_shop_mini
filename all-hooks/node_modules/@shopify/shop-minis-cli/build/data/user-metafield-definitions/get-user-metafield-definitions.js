import { graphql } from '../types/autogenerated/shop-minis-admin-api/gql.js';
import { client } from '../gql-client.js';
const userMetafieldDefinitionsQuery = graphql(/* GraphQL */ `
  query UserMetafieldDefinitions {
    userMetafieldDefinitions {
      nodes {
        description
        name
        key
        type
      }
    }
  }
`);
export const getUserMetafiledDefinitions = async () => {
    const { userMetafieldDefinitions } = await client.request(userMetafieldDefinitionsQuery, {}, {});
    return userMetafieldDefinitions.nodes.sort((nodeA, nodeB) => nodeA.key.localeCompare(nodeB.key));
};
//# sourceMappingURL=get-user-metafield-definitions.js.map